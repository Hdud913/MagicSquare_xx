# MagicSquare_xx STEP 4 — GREEN · D-LOC-01 (`find_blank_coords`)

| 항목 | 내용 |
|------|------|
| **프로젝트** | MagicSquare_xx (`C:\DEV\MagicSquare_xx`) |
| **작성일** | 2026-06-04 |
| **단계** | STEP 4 — Entity GREEN (D-LOC-01, FR-LOC-01) |
| **브랜치** | `green` |
| **선행** | [03.REPORT.md](03.REPORT.md) RED 스켈레톤 · [docs/TODO-RED.md](../docs/TODO-RED.md) |

---

## 1. 개요

STEP 3 RED 스켈레톤 이후, **Logic Track · Entity Layer**에서 D-LOC-01을 **GREEN Phase**로 통과시켰다.

| 작업 | 상태 |
|------|------|
| RED 재확인 (의도적 FAIL / collection) | ✅ |
| `src/entity/constants.py` MagicConstant SSOT | ✅ |
| `src/entity/grid.py` — `find_blank_coords` 최소 구현 | ✅ |
| `test_d_loc_01.py` — `pytest.fail` → 실제 assert | ✅ |
| pytest PASS (대상 + Logic suite) | ✅ |
| Harness 충돌 수정 (`tests/entity/__init__.py` 제거) | ✅ |
| D-LOC-02·03 · REFACTOR | ⏳ 대기 |

---

## 2. Phase 선언

```
Phase: GREEN | Layer: entity | Track: Logic | ID: D-LOC-01
```

| 제약 | 준수 |
|------|------|
| 하드코딩·매직넘버 금지 → `entity/constants.py` SSOT | ✅ |
| E001~E005 raise/return 금지 | ✅ |
| ECB: entity → boundary/control import 금지 | ✅ |
| 이번 RED 묶음 외 ID 동시 해결 금지 | ✅ |
| assert 완화·REFACTOR 금지 | ✅ |

---

## 3. RED 재확인

| 순서 | 조건 | pytest 결과 |
|------|------|-------------|
| 1 | `src/entity` 미구현 | **ERROR** — `ModuleNotFoundError: entity.constants` |
| 2 | `constants.py`만 추가, `tests/entity/__init__.py` 존재 | **ERROR** — `tests/entity` 패키지가 `src/entity` 가림 |
| 3 | `tests/entity/__init__.py` 제거 후 (스켈레톤 유지 시) | **FAILED** 예상 — `pytest.fail("RED: D-LOC-01 — …")` |

**판정:** STEP 3 블로커(constants·import shadow) 해소 후 GREEN 진행. 스켈레톤 단계의 의도적 `pytest.fail`은 import shadow 제거 직후 별도 재실행 없이 assert 교체와 동시에 GREEN 완료.

---

## 4. 구현 요약

### 4.1 `entity/constants.py` (SSOT)

| 상수 | 값 | 용도 |
|------|-----|------|
| `GRID_SIZE` | 4 | 격자 차원 |
| `MAGIC_SUM` | 34 | 10선 합 (fixture anchor) |
| `CELL_MAX` | 16 | 완성 셀 상한 |
| `BLANK_CELL` | 0 | 빈칸 표식 (스캔) |
| `INDEX_ORIGIN` | 1 | 1-index 좌표 기준 |

### 4.2 `entity/grid.py`

```python
def find_blank_coords(grid):
    """Return 1-index (row, col) for each blank cell, row-major order."""
```

- `GRID_SIZE` 이중 루프, `BLANK_CELL` 비교
- `(row + INDEX_ORIGIN, col + INDEX_ORIGIN)` 반환
- 입력 검증·빈칸 개수 판정 없음 (Boundary 책임)

### 4.3 테스트 (D-LOC-01)

| Given | When | Then |
|-------|------|------|
| **G1** (`grid_g1`) — `0` 2개 at (2,2)·(3,3) | `find_blank_coords(grid_g1)` | `[(2, 2), (3, 3)]` row-major, 1-index |

---

## 5. pytest 검증

```powershell
cd C:\DEV\MagicSquare_xx
python -m pytest tests/entity/test_d_loc_01.py::test_d_loc_01_blank_coords_row_major -v
python -m pytest tests/entity/test_d_loc_01.py -v
python -m pytest tests/entity tests/control -v
```

| 명령 | 결과 |
|------|------|
| 단일 테스트 | **1 passed** |
| `test_d_loc_01.py` 전체 | **1 passed** |
| Logic suite (`tests/entity` + `tests/control`) | **1 passed** (회귀 없음) |

### REPL 스모크

```text
G1 grid → find_blank_coords(g) → [(2, 2), (3, 3)]
```

---

## 6. Harness 수정 (import shadow)

| 원인 | `tests/entity/__init__.py`가 namespace 패키지 `entity`로 등록되어 `pyproject.toml`의 `pythonpath = ["src"]`보다 우선 |
| 조치 | `tests/entity/__init__.py` **삭제** (pytest가 `src/entity`를 정상 import) |
| `tests/conftest.py` | 변경 없음 (`grid_g1` + `entity.constants` import 유지) |

> `tests/control/__init__.py` 등은 control 네임스페이스와 무관하여 유지.

---

## 7. 산출물 (파일)

| 경로 | 변경 |
|------|------|
| `src/entity/constants.py` | **신규** — MagicConstant SSOT |
| `src/entity/grid.py` | **신규** — `find_blank_coords` |
| `src/entity/__init__.py` | 패키지 마커 (STEP 2 이후 복구) |
| `tests/entity/test_d_loc_01.py` | `pytest.fail` → `assert` |
| `tests/entity/__init__.py` | **삭제** — import shadow 제거 |

**미수정:** `docs/TODO-RED.md` 체크박스, control/boundary, D-LOC-02·03.

---

## 8. ECB · Mock · E00x

| 항목 | 판정 |
|------|------|
| Logic Track Domain Mock 금지 | ✅ `grid_g1` 정적 fixture만 |
| Entity E001~E005 emit 금지 | ✅ |
| Entity → Boundary/Control import | ✅ 없음 |
| GREEN 범위 외 ID | ✅ D-LOC-01만 |

---

## 9. STEP 3 대비 변경

| 구분 | STEP 3 | STEP 4 |
|------|--------|--------|
| `src/entity` | constants 블로커 | `constants.py` + `grid.py` |
| D-LOC-01 테스트 | `pytest.fail` RED | **PASS** assert |
| pytest | collection ERROR | **1 passed** |
| `tests/entity/__init__.py` | 빈 패키지 (shadow) | 제거 |

---

## 10. 다음 단계

| 순서 | 작업 | Track | Layer |
|------|------|-------|-------|
| 1 | D-LOC-02 RED — G1 완성 → `[]` | Logic | entity |
| 2 | D-LOC-03 RED — G0′ row-major 순서 | Logic | entity |
| 3 | REFACTOR (suite green 유지, 동작 불변) | Logic | entity |
| 4 | M1 `D-AC1`~`D-AC4` (control) RED | Logic | control |

---

## 11. 판정

| 항목 | 판정 |
|------|------|
| D-LOC-01 GREEN | ✅ |
| Logic suite 회귀 | ✅ |
| git commit | ⏳ 사용자 요청 시 |

---

## 12. Transcript

대화 Export: [Prompting/04.REPORT.md](../Prompting/04.REPORT.md)
